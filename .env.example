# Postgres connection string for the Consensus Center metrics database (the cc_* tables).
# Use the POOLED endpoint (host contains "-pooler") so it works in Vercel's serverless runtime.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
